/** Volleyball's own pose vocabulary — opaque to the shared engine, meaningful only to this game's own rules/draw code. */
export type Pose = 'idle' | 'run' | 'jump' | 'dive' | 'spike' | 'spikeForward' | 'spikeDown' | 'spikeUp' | 'tip';
